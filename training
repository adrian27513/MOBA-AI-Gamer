python train.py --batch 32 --epochs 120 --data=data.yaml --cfg=yolov5s6.yaml --weights=LeagueAIWeights.pt --name results

python print('Setup complete. Using torch   %s %s' % (torch.__version__, torch.cuda.get_device_properties(0) if torch.cuda.is_available() else 'CPU'))